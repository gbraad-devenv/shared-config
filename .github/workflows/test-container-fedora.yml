name: Test Fedora container

on:
  workflow_dispatch:
  push:
    branches:
      - "main"
  pull_request: {}


jobs:
  test:
    name: Test pull - Fedora
    uses: ./.github/workflows/reusable-test-container.yml
    with:
      container_name: "fedora"
    secrets: inherit
