name: Test Ubuntu container

on:
  workflow_dispatch:
  push:
    branches:
      - "main"
  pull_request: {}


jobs:
  test:
    name: Test pull - Ubuntu
    uses: ./.github/workflows/reusable-test-container.yml
    with:
      container_name: "ubuntu"
    secrets: inherit
